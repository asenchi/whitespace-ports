Whitespace.io FreeBSD Ports
===========================

Various FreeBSD ports that are used in Whitespace infrastructure.

- bootstrap::
  Meta-port that installs the necessary ports for a base server configuration.
